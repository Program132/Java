public class div {
    public static void main( String [] args ) {
        int a = 127, b = 100, result; // On peut déclaré plusieurs variable de même type en une seule ligne !
        result = a / b; //pour diviser on met /
    }
}
